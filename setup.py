from setuptools import setup

setup(
   name='src',
   version='1.0',
   description='forecasting modules',
   author='Lisa Team',
   author_email='PT-DLS-SQL@bcn.bosch.com',
   packages=['src'],  #same as name
   install_requires=[], #external packages as dependencies
)
